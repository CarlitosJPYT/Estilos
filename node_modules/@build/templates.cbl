       IDENTIFICATION DIVISION.
       PROGRAM-ID. CAJERO.
       DATA DIVISION.
       FILE SECTION.
       WORKING-STORAGE SECTION.
       01 SALDO_INICIAL  PIC 9(4) VALUE 1000.
       01 SALDO          PIC 9(4) VALUE ZERO.
       01 RETIRO         PIC 9(4) VALUE ZERO.
       01 OPCION         PIC 9(4) VALUE ZERO.
       PROCEDURE DIVISION.
       MAIN-PROCEDURE.
            DISPLAY "BIENVENIDO A SU CAJERO VIRTUAL".
            DISPLAY "1. INGRESE LA CANTIDAD A RETIRAR:".
            DISPLAY "2. INGRESE LA CANTIDAD A DEPOSITAR:".
            DISPLAY "3. CONSULTAR SALDO:".
            DISPLAY "4. SALIR:".
            DISPLAY "INGRESE UNA OPCION (1-4):".
            ACCEPT OPCION.
            EVALUATE OPCION
              WHEN 1
                 DISPLAY "INGRESE LA CANTIDAD A RETIRAR:"
                 ACCEPT RETIRO
                 IF RETIRO GREATER THAN SALDO_INICIAL THEN
                    DISPLAY "NO TIENE SUFICIENTE SALDO PARA RETIRAR."
                    ELSE
                       COMPUTE SALDO = SALDO_INICIAL - RETIRO
                       DISPLAY "SU NUEVO SALDO ES: " SALDO
                 END-IF
                 PERFORM MAIN-PROCEDURE
              WHEN 2
                 DISPLAY "INGRESE LA CANTIDAD A DEPÓSITAR:"
                 ACCEPT RETIRO
                 COMPUTE SALDO = SALDO_INICIAL + RETIRO
                 DISPLAY "SU NUEVO SALDO ES: " SALDO
                 PERFORM MAIN-PROCEDURE
              WHEN 3
                 DISPLAY "SU SALDO ACTUAL ES: " SALDO
                 PERFORM MAIN-PROCEDURE
              WHEN 4
                 DISPLAY "GRACIAS POR USAR EL CAJERO VIRTUAL."
                 STOP RUN
              WHEN OTHER
                 DISPLAY "OPCION NO VALIDA, POR FAVOR INTENTE DE NUEVO.".
                 PERFORM MAIN-PROCEDURE
            END-EVALUATE.
            STOP RUN.
       END PROGRAM CAJERO.
